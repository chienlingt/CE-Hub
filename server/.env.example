DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/DATABASE?schema=public
PORT=4000
CLIENT_URL=http://localhost:3000

# Server-side B.2 calls: enable Geocoding API, Distance Matrix API and Routes API.
# Restrict this key by server IP and API; never expose it to the browser.
GOOGLE_MAPS_SERVER_KEY=
